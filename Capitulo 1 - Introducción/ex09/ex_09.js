/* Arrays o arreglos

Un aray es una estructura de datos que se utiliza para almacenar una selección ordenada de elementos.
Estos elementos pueden ser cadenas de texto, valores númericos como: enteros, decimales, valores booleanos, entre otros. Pueden ser también un conjunto de funciones e inclusive otros arrays. 

*/

let numeros = []; // Declaración de un array vacío.

numeros = [15, 80, 650, 50.43, -10]; // Se guarda n el array una colección númerica de tipo decimal o entero.
console.log(numeros); // Se imprime por consola todo el array.
console.log(numeros[3]); // Se imprime por consola el cuarto elemento que pertenece al indicde número 3.

